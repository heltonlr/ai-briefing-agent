import React from "react";
import ReactDOM from "react-dom/client";
import BriefingAgent from "./BriefingAgent.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BriefingAgent />
  </React.StrictMode>
);
